# Changelog

## 0.1.0
- Initial release
- `loadConfig()` with schema validation and type coercion
- `envSource()` and `objectSource()` config sources
- Required fields, defaults, and env variable mapping
